apiVersion: v1
kind: Pod
metadata:
  name: irsa-test
  namespace: vault
spec:
  serviceAccountName: vault
  containers:
  - name: aws-cli
    image: amazon/aws-cli
    command: ["sh", "-c", "aws sts get-caller-identity && sleep 3600"]
